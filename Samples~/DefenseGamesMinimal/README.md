# Defense Games Minimal Sample

This sample shows the package in its smallest useful form: consume an encounter spawn request, assign a route, report a kill, and inspect the runtime snapshot.

Copy `DefenseGamesMinimalSample.cs` into a Unity scene or EditMode test assembly that references `com.deucarian.defense-games`.
